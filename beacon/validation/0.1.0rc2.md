# Beacon Python 0.1.0rc2 验收记录

## 结论

`beacon-otel==0.1.0rc2` 与 `beacon-profiling==0.1.0rc2` 验收通过。

- 公开 PyPI 制品可以安装并启动 FastAPI Demo。
- 未设置 `OTEL_PROFILING_EXPORT_INTERVAL` 时，DataKit 收到的 Profile 持续时间为 60 秒。
- Profile 包含真实 CPU Time、Wall Time 与 Heap Live Size。
- 兼容 pprof 不再输出无法正确表达的 `alloc-samples` 和 `alloc-space`。
- Profile 不包含 `OTelProfileScheduler` 采集线程。
- Demo 请求、Trace 上报与 Profile 上报均成功。

## 验收范围

- 时间：2026-09-23 11:30:00–11:35:00 +08:00
- 服务：`beacon-python-rc2-validation`
- 环境：`demo`
- Python：CPython 3.13.12
- OpenTelemetry Python SDK：1.44.0
- DataKit：1.94.1
- 数据域：APM Trace、Profile

## Profile 证据

DataKit 中的 Profile 记录：

- Profile ID：`L9y_c1AXSzG84qlJPYSXMA`
- 开始时间：2026-09-23 11:31:28 +08:00
- 结束时间：2026-09-23 11:32:28 +08:00
- 持续时间：60 秒
- Profiler 版本：`0.1.0rc2`
- 服务版本：`0.1.0rc2`

同一窗口的本地 pprof 解析结果：

- 持续时间：60.311 秒
- 样本数：14,513
- `cpu-samples`：3,691
- `cpu-time`：27,745,635,212 纳秒
- `wall-time`：142,980,000,000 纳秒
- `heap-space`：26,305,783 字节
- 不包含 `alloc-samples`、`alloc-space` 或 `OTelProfileScheduler`

## Trace 证据

验收期间共执行 8 次 `/work` 请求，全部返回 HTTP 200 并产生有效 Trace。APM 查询确认：

- `service`：`beacon-python-rc2-validation`
- `environment`：`demo`
- `service.version`：`0.1.0rc2`
- `/work`、`/health`、HTTP Client 和内部发送 Span 均可查询
- 已抽查完整父子链路

## 已知限制

- 当前 DataKit 接收路径使用兼容 pprof，而不是 OTLP Profiles v1development。
- `tracemalloc` 在大量小对象和很短的内存快照周期下开销明显；默认周期为 60 秒，可通过环境变量或构造参数调整。
- OpenTelemetry Resource 属性进入 DataKit 后会按 DataKit Trace 字段模型归一化，非内置标签可能保留在扩展属性中。
